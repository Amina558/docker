# 🉑Docker File Usage
     ▶️In this part we usually discuss about the 
     . Docker file


 # 🉑What is Docker file →   
     .A Dockerfile is a text file that contains a set of instructions for building a Docker i creates a new layer on top of the previous one, allowing for efficient image building and caching.
